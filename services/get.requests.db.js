module.exports = function(pool){

async function coinID(coin){
    try{
        let id = await pool.query(`select `)
    }
    catch(err){
        console.error(err)
    }
}

return{
    coinID
}
}